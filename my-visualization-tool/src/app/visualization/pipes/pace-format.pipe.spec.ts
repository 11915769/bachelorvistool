import { PaceFormatPipe } from './pace-format.pipe';

describe('PaceFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PaceFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
